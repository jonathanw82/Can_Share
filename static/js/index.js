

function vote(direction){
    if (direction == 'up'){
        // xhttp request url passin 1 and can_id  
        // results python going to check if the user has already rated it and determin what action to take
        // return that action update icon depending on outcome.
    } 
    else {
         // xhttp request url passin 0 and can_id  
        // results python going to check if the user has already rated it and determin what action to take
        // return that action
    }
}
